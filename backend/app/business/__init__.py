"""Business Layer.

Contains the main application logic that connects user specifications to
features. Text and voice requests are normalized into a shared request format
and processed through the same workflow. The Business Layer asks the
Persistence Layer for data and returns structured results.
"""
